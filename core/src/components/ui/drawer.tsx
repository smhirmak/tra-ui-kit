// Re-export from registry (single source of truth)
// Main code is in registry/tra-kit/components/drawer.tsx
// This allows registry build to work properly while keeping imports clean
export * from '../../../registry/tra-kit/components/drawer';
